/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { messagesEmailSend } from "../../funcs/messagesEmailSend.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SendEmailRequest$inboundSchema,
};

export const tool$messagesEmailSend: ToolDefinition<typeof args> = {
  name: "messages-email-send",
  description: `Send an Email

Send an email from the PushPress platform. Note that only first party apps can send emails`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await messagesEmailSend(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
