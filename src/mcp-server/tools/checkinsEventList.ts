/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { checkinsEventList } from "../../funcs/checkinsEventList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListEventCheckinsRequest$inboundSchema,
};

export const tool$checkinsEventList: ToolDefinition<typeof args> = {
  name: "checkins-event-list",
  description: `List Event Checkins

List event checkins. Includes details about the event`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checkinsEventList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
