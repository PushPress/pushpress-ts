/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { checkinsAppointmentGet } from "../../funcs/checkinsAppointmentGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAppointmentCheckinRequest$inboundSchema,
};

export const tool$checkinsAppointmentGet: ToolDefinition<typeof args> = {
  name: "checkins-appointment-get",
  description: `Get Appointment Checkin Details

Get the checkin details for appointment including appointment details and checkin time`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checkinsAppointmentGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
