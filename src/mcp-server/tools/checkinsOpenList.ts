/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { checkinsOpenList } from "../../funcs/checkinsOpenList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListOpenCheckinsRequest$inboundSchema,
};

export const tool$checkinsOpenList: ToolDefinition<typeof args> = {
  name: "checkins-open-list",
  description: `List Open Checkins

List open facility checkins`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checkinsOpenList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
