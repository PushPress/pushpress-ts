/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { checkinsCount } from "../../funcs/checkinsCount.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CountCheckinsRequest$inboundSchema,
};

export const tool$checkinsCount: ToolDefinition<typeof args> = {
  name: "checkins-count",
  description: `Get Checkin Count

Get a count of checkins based on filter criteria, including event, customer and time range`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checkinsCount(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
