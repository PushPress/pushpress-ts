/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { manageWebhooksCreate } from "../../funcs/manageWebhooksCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateWebhookRequest$inboundSchema,
};

export const tool$manageWebhooksCreate: ToolDefinition<typeof args> = {
  name: "manage-webhooks-create",
  description: `Create a Webhook

Create a platform webhook that can be used to listen for events on the pushpress platform at a given URL`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await manageWebhooksCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
