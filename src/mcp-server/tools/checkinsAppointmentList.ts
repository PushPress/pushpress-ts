/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { checkinsAppointmentList } from "../../funcs/checkinsAppointmentList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAppointmentCheckinsRequest$inboundSchema,
};

export const tool$checkinsAppointmentList: ToolDefinition<typeof args> = {
  name: "checkins-appointment-list",
  description: `List Appointment Checkins

list appointment checkins. Includes details about the appointment, customer and staff`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checkinsAppointmentList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
