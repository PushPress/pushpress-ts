/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { invitationsCreate } from "../../funcs/invitationsCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateInvitationsRequest$inboundSchema,
};

export const tool$invitationsCreate: ToolDefinition<typeof args> = {
  name: "invitations-create",
  description: `Create Invitations

Invite a list of customers to an event`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await invitationsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
