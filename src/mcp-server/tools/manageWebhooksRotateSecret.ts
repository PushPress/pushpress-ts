/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { manageWebhooksRotateSecret } from "../../funcs/manageWebhooksRotateSecret.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RotateWebhookSigningSecretRequest$inboundSchema,
};

export const tool$manageWebhooksRotateSecret: ToolDefinition<typeof args> = {
  name: "manage-webhooks-rotate-secret",
  description: `Rotate a Webhook Signing Secret

Rotate a platform webhook's signing secret`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await manageWebhooksRotateSecret(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
