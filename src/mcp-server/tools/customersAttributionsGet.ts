/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customersAttributionsGet } from "../../funcs/customersAttributionsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAttributionRequest$inboundSchema,
};

export const tool$customersAttributionsGet: ToolDefinition<typeof args> = {
  name: "customers-attributions-get",
  description: `Get a customer attribution

Get a customer attribution by UUID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customersAttributionsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
