/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { classesTypeList } from "../../funcs/classesTypeList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListClassTypesRequest$inboundSchema,
};

export const tool$classesTypeList: ToolDefinition<typeof args> = {
  name: "classes-type-list",
  description: `List class types for the given gym

Get Class type details`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await classesTypeList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
