/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { invitationsDelete } from "../../funcs/invitationsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteInvitationByIdRequest$inboundSchema,
};

export const tool$invitationsDelete: ToolDefinition<typeof args> = {
  name: "invitations-delete",
  description: `Delete an invitation

Delete an invitation by its id`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await invitationsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
