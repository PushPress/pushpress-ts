/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { manageWebhooksDeactivate } from "../../funcs/manageWebhooksDeactivate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeactivateWebhookRequest$inboundSchema,
};

export const tool$manageWebhooksDeactivate: ToolDefinition<typeof args> = {
  name: "manage-webhooks-deactivate",
  description: `Deactivate a Webhook

deactivate a platform webhook. If you want to fully delete a webhook use the delete method`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await manageWebhooksDeactivate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
