/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companyGet } from "../../funcs/companyGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCompanyRequest$inboundSchema,
};

export const tool$companyGet: ToolDefinition<typeof args> = {
  name: "company-get",
  description: `Get Company Details

Fetch the company details associated with the currently authenticated user.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await companyGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
