/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { messagesRealtimeSend } from "../../funcs/messagesRealtimeSend.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SendEventRequest$inboundSchema,
};

export const tool$messagesRealtimeSend: ToolDefinition<typeof args> = {
  name: "messages-realtime-send",
  description: `Send Ably Realtime Event

Send an event to a websocket channel via Ably Realtime. Note that only first party apps can send realtime notifications`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await messagesRealtimeSend(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
