/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { checkinsOpenGet } from "../../funcs/checkinsOpenGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOpenCheckinRequest$inboundSchema,
};

export const tool$checkinsOpenGet: ToolDefinition<typeof args> = {
  name: "checkins-open-get",
  description: `Get Open Facility Details

Get the checkin details for an open facility checkin`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await checkinsOpenGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
