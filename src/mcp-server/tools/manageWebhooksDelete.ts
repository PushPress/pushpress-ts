/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { manageWebhooksDelete } from "../../funcs/manageWebhooksDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteWebhookRequest$inboundSchema,
};

export const tool$manageWebhooksDelete: ToolDefinition<typeof args> = {
  name: "manage-webhooks-delete",
  description: `Delete a Webhook

Fully delete a platform webhook. If you want to unsubscribe to a webhook without fully deleting it, use the deactivate method instead`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await manageWebhooksDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
