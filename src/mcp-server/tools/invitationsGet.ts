/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { invitationsGet } from "../../funcs/invitationsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetInvitationByIdRequest$inboundSchema,
};

export const tool$invitationsGet: ToolDefinition<typeof args> = {
  name: "invitations-get",
  description: `Get an invitation

Get an invitation for an event by its id`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await invitationsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
