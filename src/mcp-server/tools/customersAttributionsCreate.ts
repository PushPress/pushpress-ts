/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customersAttributionsCreate } from "../../funcs/customersAttributionsCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateAttributionRequest$inboundSchema,
};

export const tool$customersAttributionsCreate: ToolDefinition<typeof args> = {
  name: "customers-attributions-create",
  description: `Create a customer attribution

Create a new customer attribution record`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customersAttributionsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
