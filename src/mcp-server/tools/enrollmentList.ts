/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { enrollmentList } from "../../funcs/enrollmentList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListEnrollmentsRequest$inboundSchema,
};

export const tool$enrollmentList: ToolDefinition<typeof args> = {
  name: "enrollment-list",
  description: `List Plan Enrollments

Get a list of enrollments in the current company`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await enrollmentList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
